This folder contains a HelloWorld class which has an empty sayIt() method. The Instrument class modifies the body of thhe sayIt() method first by adding a system.out.println and then calls the method. To run use the ./build.sh script.

